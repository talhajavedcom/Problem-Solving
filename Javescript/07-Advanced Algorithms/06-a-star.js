// Implement A* algorithm
// Find shortest path using heuristic function

// Sample test cases:
// Grid: 0=walkable, 1=obstacle
// Start: (0,0), Goal: (3,3)
// Heuristic: Manhattan distance
// Result: shortest path avoiding obstacles

// TODO: Implement A* algorithm
// Hint: Use priority queue with f(n) = g(n) + h(n)

// Your implementation should handle:
// - Grid-based map
// - Start and goal positions
// - Obstacles
// - Heuristic function
// - Path reconstruction

// Note: f(n) = g(n) + h(n) where g(n) is cost from start, h(n) is heuristic
