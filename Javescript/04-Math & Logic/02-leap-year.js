// Check if a year is leap year
// Leap year rules: divisible by 4, but not by 100 unless also by 400

// Sample test cases:
// 2000 -> true (divisible by 400)
// 2020 -> true (divisible by 4, not by 100)
// 1900 -> false (divisible by 100, not by 400)
// 2021 -> false (not divisible by 4)
// 1600 -> true (divisible by 400)
// 1700 -> false (divisible by 100, not by 400)

// TODO: Implement function to check leap year
// Hint: Use modulo operator (%) and logical operators

// Your function should handle:
// - Years divisible by 4
// - Years divisible by 100
// - Years divisible by 400
// - Regular years
// - Negative years (consider if valid)

// Note: Leap year rules:
// 1. Year must be divisible by 4
// 2. If divisible by 100, must also be divisible by 400
