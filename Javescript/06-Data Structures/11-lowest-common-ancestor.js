// Find lowest common ancestor in BST
// LCA is the lowest node that has both nodes as descendants

// Sample test cases:
// Tree: 6 -> [2, 8], 2 -> [0, 4], 8 -> [7, 9]
// LCA(2, 8) -> 6
// LCA(2, 4) -> 2
// LCA(0, 5) -> 2
// LCA(7, 9) -> 8

// TODO: Implement function to find LCA
// Hint: Use BST property to navigate down the tree

// Your function should handle:
// - Both nodes exist
// - One node doesn't exist
// - Both nodes don't exist
// - Nodes are same
// - Root is LCA
// - Empty tree

// Note: Use BST property: left < root < right
