// Implement event emitter
// Create a simple event emitter with on, emit, and off methods

// Sample test cases:
// emitter.on('event', callback)
// emitter.emit('event', data) -> calls callback with data
// emitter.off('event', callback) -> removes callback
// Multiple listeners for same event
// Emit to non-existent event

// TODO: Implement EventEmitter class with methods:
// - on(event, callback)
// - emit(event, ...args)
// - off(event, callback)

// Your implementation should handle:
// - Adding event listeners
// - Removing event listeners
// - Emitting events
// - Multiple listeners per event
// - No listeners for event
// - Error handling

// Note: Store callbacks in a map or object
