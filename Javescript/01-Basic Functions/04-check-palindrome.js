// Check whether a string is palindrome
// A palindrome reads the same forward and backward (ignoring case, spaces, punctuation)

// Sample test cases:
// "racecar" -> true
// "A man a plan a canal Panama" -> true
// "hello" -> false
// "Madam" -> true
// "12321" -> true
// "Was it a car or a cat I saw?" -> true
// "" -> true (empty string is palindrome)
// "a" -> true (single character is palindrome)

// TODO: Implement palindrome checking function
// Hint: You can use string methods like toLowerCase(), replace(), split(), reverse(), join()

// Your function should handle:
// - Case insensitivity (Madam = madam)
// - Spaces and punctuation
// - Empty strings
// - Single characters
// - Numbers as strings
// - Special characters

// Note: Consider whether you want to ignore spaces and punctuation or not
