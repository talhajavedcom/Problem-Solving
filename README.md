# 📚 JavaScript Assignments – Practice Tracker

A complete list of JavaScript coding challenges with **sample data** for practice.  

---

## 📌 Basic Functions

- [ ] **Find maximum of two numbers**  
   ```js
   maxOfTwo(10, 25) // → 25
   ```

- [ ] **Check whether a number is even or odd**  
   ```js
   isEven(7)   // → false
   isEven(12)  // → true
   ```

- [ ] **Reverse a string**  
   ```js
   reverseString("hello") // → "olleh"
   ```

---

## 📌 Array Problems

- [ ] **Find the largest number in an array**  
   ```js
   largest([10, 5, 20, 8, 15]) // → 20
   ```

- [ ] **Remove duplicates from an array**  
   ```js
   removeDuplicates([1, 2, 2, 3, 4, 4, 5]) // → [1, 2, 3, 4, 5]
   ```

---

## 📌 String Problems

- [ ] **Check if two strings are anagrams**  
   ```js
   isAnagram("listen", "silent") // → true
   isAnagram("hello", "world")   // → false
   ```

- [ ] **Count vowels in a string**  
   ```js
   countVowels("hello world") // → 3
   ```

---

## 📌 Sorting

- [ ] **Bubble Sort**  
   ```js
   bubbleSort([5, 3, 8, 4, 2]) // → [2, 3, 4, 5, 8]
   ```

---

✨ Continue similarly for 100+ assignments…
