// Count consonants in a string
// Return the number of consonants (non-vowels) in a string

// Sample test cases:
// "hello" -> 3 (h, l, l)
// "world" -> 4 (w, r, l, d)
// "aeiou" -> 0 (no consonants)
// "xyz" -> 3 (x, y, z)
// "JavaScript" -> 7 (J, v, S, c, r, p, t)
// "123" -> 0 (no letters)
// "" -> 0 (empty string)

// TODO: Implement function to count consonants
// Hint: You can use regex, includes(), or for loop

// Your function should handle:
// - Lowercase strings
// - Uppercase strings
// - Mixed case strings
// - Empty strings
// - Strings with no consonants
// - Strings with only consonants
// - Numbers and special characters
